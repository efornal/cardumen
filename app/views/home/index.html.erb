<div class="bs-docs-section">

  <div class="row">
    <div class="col-lg-12">
      <br>
      <div class="bs-component">
        <table class="table table-striped table-hover ">
          <thead>
            <tr>
              <th>Empleado</th>
              <th>Oficina</th>
              <th>Teléfono Oficina</th>
              <th>Interno</th>
            </tr>
          </thead>
          <tbody>
            <% @offices.each do |office| %>
              <% office.employees.order("surname ASC").each do |employee|  %>
                <tr>
                <td><%= employee.name_and_surname %></td>
                <td><%= office.name %></td>
                <td><%= office.phone %></td>
                <td><%= employee.phone %></td>
                </tr>
              <% end %>
            <% end %>
          </tbody>
        </table> 
      </div>
    </div>
  </div>
</div>
